<style>
    body {
        font-family: 'Poppins', sans-serif;
        margin: 0;
        padding: 0;
        height: 100vh;
        background: url('/images/background.png') no-repeat center center fixed;
        background-size: cover;
        display: flex;
    }

    /* Sidebar Styling */
    .sidebar {
        width: 260px;
        background: #2c3e50;
        color: white;
        height: 100vh;
        padding: 20px;
        position: fixed;
        left: -250px;
        /* Initially collapsed */
        top: 0;
        box-shadow: 2px 0 5px rgba(0, 0, 0, 0.1);
        transition: left 0.3s ease-in-out;
        display: flex;
        flex-direction: column;
        justify-content: flex-start;
    }

    .sidebar.active {
        left: 0;
    }

    /* Menu Toggle Button */
    .menu-toggle {
        position: fixed;
        top: 15px;
        left: 15px;
        background: #3498db;
        color: white;
        border: none;
        padding: 10px 15px;
        cursor: pointer;
        border-radius: 5px;
        z-index: 1001;
    }

    /* Logo Styling */
    .sidebar .logo-main {
        display: flex;
        align-items: center;
        justify-content: flex-start;
        position: absolute;
        top: 20px;
        left: 70px;
        height: 40px;
        width: 160px;
    }

    .sidebar .brand-name {
        margin-left: 5px;
    }

    .sidebar .logo-main img {
        width: 20px;
        height: 20px;
        object-fit: contain;
    }

    /* Sidebar Menu */
    .sidebar .sidebar-menu {
        list-style: none;
        padding: 0;
        margin-top: 70px;
    }

    .sidebar .menu-item {
        margin: 15px 0;
    }

    .sidebar .menu-link {
        text-decoration: none;
        color: white;
        display: block;
        padding: 10px;
        border-radius: 5px;
        transition: background 0.3s ease;
        cursor: pointer;
        /* Indicate clickable for categories */
    }

    .sidebar .menu-link:hover {
        background: #3498db;
    }

    /* Sidebar Menu */
    .sidebar .sidebar-menu {
        list-style: none;
        padding: 0;
        margin-top: 70px;
    }

    .sidebar .menu-item {
        margin: 15px 0;
    }

    .sidebar .menu-link {
        text-decoration: none;
        color: white;
        display: block;
        padding: 10px;
        border-radius: 5px;
        transition: background 0.3s ease;
        cursor: pointer;
        position: relative;
        /* For positioning the arrow */
    }

    .sidebar .menu-link:hover {
        background: #3498db;
    }

    /* Arrow Indicator for Submenu Items */
    .sidebar .has-submenu .menu-link::after {
        content: '▼';
        /* Downward arrow */
        position: absolute;
        right: 10px;
        font-size: 0.8em;
        transition: transform 0.3s ease;
        /* Smooth rotation */
    }

    .sidebar .has-submenu.active .menu-link::after {
        transform: rotate(180deg);
        /* Upward arrow when expanded */
    }

    /* Submenu Styling */
    .sidebar .submenu {
        list-style: none;
        padding: 0;
        margin: 0;
        display: none;
        /* Hidden by default */
        background: #34495e;
        border-radius: 5px;
    }

    .sidebar .submenu li {
        margin: 5px 0;
    }

    .sidebar .submenu li a {
        text-decoration: none;
        color: white;
        display: block;
        padding: 8px 15px 8px 25px;
        /* Indent subitems */
        border-radius: 5px;
        transition: background 0.3s ease;
    }

    .sidebar .submenu li a:hover {
        background: #3498db;
    }

    /* Show submenu when active */
    .sidebar .has-submenu.active .submenu {
        display: block;
    }

    /* When sidebar is collapsed, hide the menu items */
    .sidebar.collapsed .sidebar-menu {
        display: none;
    }

    /* When the sidebar is active and open, show the items */
    .sidebar.active.collapsed .sidebar-menu {
        display: block;
    }

    /* Content Wrapper */
    .main-content {
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        width: 100%;
        min-height: 100vh;
        text-align: center;
        padding: 20px;
        transition: margin-left 0.3s ease-in-out;
    }

    .main-content.shift {
        margin-left: 250px;
    }

    /* Title Styling */
    .main-content h1 {
        font-size: clamp(2rem, 4vw, 4.5rem);
        font-weight: bold;
        color: #000000;
        text-shadow: 2px 2px 5px rgba(0, 255, 0, 0.5);
        text-align: center;
        margin-top: 10px;
        word-wrap: break-word;
        max-width: 90%;
    }

    /* Subtitle Styling */
    .main-content p {
        font-size: clamp(1rem, 2vw, 1.5rem);
        color: #000000;
        margin-bottom: 30px;
        max-width: 80%;
        word-wrap: break-word;
    }

    /* Mobile & Tablet Styles */
    @media (max-width: 768px) {
        .sidebar {
            width: 260px;
            transform: translateX(-100%);
            height: 100vh;
            position: fixed;
            z-index: 1000;
        }

        .main-content {
            width: 100%;
            padding: 20px;
        }

        .sidebar.active {
            transform: translateX(0);
        }

        .main-content.shift {
            margin-left: 260px;
        }

        .menu-toggle {
            position: fixed;
            top: 15px;
            left: 15px;
            background: #3498db;
            color: white;
            border: none;
            padding: 10px;
            cursor: pointer;
            border-radius: 5px;
            z-index: 1001;
        }
    }


    .user-greeting {
        position: fixed;
        top: 15px;
        right: 15px;
        display: flex;
        align-items: center;
        gap: 10px;
        z-index: 1001;
    }

    .user-greeting span {
        color: #000000;
        font-size: 1.2em;
        font-weight: bold;
    }

    .sign-out-btn {
        background: #e74c3c;
        color: white;
        border: none;
        padding: 10px 15px;
        cursor: pointer;
        border-radius: 5px;
        font-size: 1em;
        font-weight: bold;
        transition: background 0.3s ease;
    }

    .sign-out-btn:hover {
        background: #c0392b;
    }
</style>